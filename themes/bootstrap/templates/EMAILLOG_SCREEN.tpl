{TITLE}

{RESULTS_TABLE}

<div class="btn-toolbar">
	<form title="{!DELETE_ALL}" class="pull-right" action="{MASS_DELETE_URL*}" method="post">
		<div class="inline">
			<input class="btn btn-primary btn-page" type="submit" value="{!DELETE_ALL}" />
		</div>
	</form>
	<form title="{!SEND_ALL}" class="pull-right" action="{MASS_SEND_URL*}" method="post">
		<div class="inline">
			<input class="btn btn-primary btn-page" type="submit" value="{!SEND_ALL}" />
		</div>
	</form>
</div>
